mutation updateNote(
    $id: ID!
    $title: String!
    $body: String!
    $isFeatured: Boolean
) {
    updateNote(
        id: $id
        title: $title
        body: $body
        isFeatured: $isFeatured
    ) {
        _id
        title
        body
    }
}